# Работа: офисная сеть за VPN-сервером компании, доступная по имени и по адресу
# откуда угодно.
#
# Снова заглушки: corp.example.com — вместо внутреннего домена, подсеть —
# вместо офисной сети. Частная подсеть здесь не должна пересекаться с сетью,
# в которой сейчас Mac, иначе маршрут уведёт в туннель локальные узлы.
corp.example.com
git.example.com
172.20.0.0/16
