# Kraken: сайт биржи и её API.
#
# Один суффикс покрывает сайт, спотовый API (api.kraken.com) и API фьючерсов
# (futures.kraken.com). Переедет сервис биржи на новый домен — добавьте сюда
# строку, работающая служба подхватит изменение сама.
kraken.com
